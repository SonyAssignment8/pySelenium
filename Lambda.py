def add(a, b):
    return a + b
# print(add(1, 2))    # normal steps which we follow

z = lambda a, b : a + b  # Its using Lambda Expression
print(z(1, 2))

# pass the argument with lambda
# put colon and then write operation which you need to perform
# store all these in one variable (Z)
# pass the value with print statement

